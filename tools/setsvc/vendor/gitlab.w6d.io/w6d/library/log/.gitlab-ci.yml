variables:
  REGISTRY: reg-ext.w6d.io
  CONTAINER_IMAGE: ${REGISTRY}/${CI_PROJECT_PATH}
  DOCKER_DRIVER: overlay2
  JOB_USER: "gitlab-ci-token"

stages:
  - rollout

include:
  - project: 'library/stages'
    ref: v0.5.5
    file: '/gitlab/helm/rollout-prod.yaml'
  - project: 'library/stages'
    ref: v0.5.5
    file: '/gitlab/footer.yaml'
